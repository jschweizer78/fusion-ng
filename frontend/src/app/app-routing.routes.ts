import { Routes, RouterModule } from '@angular/router';
import { FusionMainComponent } from './fusion-main/fusion-main.component';

export const routes: Routes = [
  { path: '', component: FusionMainComponent }
];
